{
  "permissions": {
    "allow": [
      "mcp__Claude_Preview__preview_start",
      "Bash(pkill -f \"npx serve\")",
      "Bash(pkill -f \"serve -l 3456\")",
      "Bash(python3 -c \":*)"
    ]
  }
}
